node_modules
dist
.env
.env.local
.env.development
.env.*.local
# Cloudflare Worker local secrets (never commit). The .example template IS committed.
.dev.vars
*.dev.vars
!*.dev.vars.example
*.log
.vite
.DS_Store

# Supabase local dev (keep migrations and config)
supabase/.temp
supabase/.branches

# Local Claude Code agent settings (per-machine, not project)
.claude/
.playwright-mcp/
